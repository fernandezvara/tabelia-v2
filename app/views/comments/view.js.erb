<% @comments.each do |comment| %>
	$("ul.comments").append("<%= escape_javascript(render :partial => 'partials/user/profile/comment', :locals => { :comment => comment }) %>");
<% end %>
	<% if @nolink == true %>
		$("p.more").hide();
	<% else %>
		$("p.more").html("<%= escape_javascript(link_to('Ver más', view_more_comments_path(:username => @user.username, :last => @last_comment_time), :class=> 'abutton', :remote => true, :walk => 'no')) %>");
	<% end %>
