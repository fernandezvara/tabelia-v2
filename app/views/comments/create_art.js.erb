$('ul.comments').prepend("<%= escape_javascript(render :partial => 'partials/art/profile/comment', :locals => { :comment => @comment}) %>");
$('textarea#text').val("Escribe un comentario");
flashBar("Comentario añadido correctamente.", "success", {});