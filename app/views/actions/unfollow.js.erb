<% if @result == "true" %>
	$("div#follow_status").html("<%= escape_javascript(link_to(t('common.follow'), action_follow_path(:username => @user.username), :remote => true, :class => ['abutton', 'follow'], :walk => 'no')) %>");
	$("div#follow_info").html("<%= escape_javascript(render :partial => 'partials/user/profile/followinfo', :locals => { :user => @user}) %>")
	flashBar("You stopped following <%= @user.name %>,", "notice", {});
<% else %>
	alert("Error inesperado.");
<% end %>